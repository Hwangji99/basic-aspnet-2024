@model MyPortfolio.Models.User

@{
    ViewData["Title"] = "Register";
}

<style>
    .my-container {
        max-width: 800px;
    }
</style>

<main class="container px-5 my-container">
    <div class="py-5 text-center">
        <img class="d-block mx-auto mb-4" src="~/assets/bootstrap-logo.svg" alt="" width="72" height="57">
        <h2>회원가입</h2>
        <p class="lead">저의 사이트에 와주셔서 감사합니다.<br /> 아래의 내용을 입력하고 회원가입 해주시기 바랍니다.</p>
    </div>

    <div class="row g-5">

        <div class="col">
            <h4 class="mb-3">기본입력사항</h4>
            <form class="needs-validation" asp-controller="Home" asp-action="Register" novalidate>
                <div class="row g-3">
                    <div class="col-12">
                        <label asp-for="UserEmail" class="form-label">이메일 주소</label>
                        @*input 태그의 asp-for는 HTML로 렌더링된 뒤 id=와 name으로 변환됨*@
                        <input type="email" class="form-control" asp-for="UserEmail" placeholder="you@example.com" required>
                        <div class="invalid-feedback">
                            이메일 주소는 필수입니다.
                        </div>
                    </div>

                    <div class="col-12">
                        <label asp-for="UserName" class="form-label">사용자명</label>
                        <div class="input-group has-validation">
                            <span class="input-group-text">&#64;</span>
                            <input type="text" class="form-control" asp-for="UserName" placeholder="홍길동" required>
                            <div class="invalid-feedback">
                                사용자명은 필수입니다.
                            </div>
                        </div>
                    </div>

                    <div class="col-12">
                        <label asp-for="Address" class="form-label">주소<span class="text-body-secondary">(옵션)</span></label>
                        <input type="text" class="form-control" asp-for="Address" placeholder="서울시 도봉구">
                    </div>

                    <div class="col-12">
                        <label asp-for="PhoneNum" class="form-label">전화번호<span class="text-body-secondary">(옵션)</span></label>
                        <input type="text" class="form-control" asp-for="PhoneNum" placeholder="010-6666-7777">
                    </div>

                    <div class="col-12">
                        <label asp-for="Password" class="form-label">비밀번호</label>
                        <input type="password" class="form-control" asp-for="Password" placeholder="특수문자1개이상, 숫자, 영문자포함 8자 이상" required>
                        <div class="invalid-feedback">
                            비밀번호는 필수입니다.
                        </div>
                    </div>


                    <div class="col-12">
                        <label asp-for="PasswordCheck" class="form-label">비밀번호 확인</label>
                        <input type="password" class="form-control" asp-for="PasswordCheck" required>
                        <div class="invalid-feedback">
                            비밀번호 확인은 필수입니다.
                        </div>
                        <span asp-validation-for="PasswordCheck" class="text-danger"></span>
                    </div>

                </div>

                <hr class="my-4">

                <button class="w-100 btn btn-primary btn-lg" type="submit">회원가입</button>
            </form>
        </div>
    </div>
</main>

<script src="/js/checkout.js"></script>
